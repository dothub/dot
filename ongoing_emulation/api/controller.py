class Controller(object):
    def __init__(self, ctrl_id, ctrl_ip, ctrl_port, in_band):
        self.ctrl_id = ctrl_id
        self.ip = ctrl_ip
        self.port = ctrl_port
        self.in_band = in_band
         
